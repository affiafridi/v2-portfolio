'use client'

/**
 * Returns the page scroll progress as a value between 0 and 1.
 * TODO: attach scroll listener and compute scrollY / (scrollHeight - innerHeight).
 */
export function useScrollProgress(): number {
  // TODO: implement
  return 0
}
